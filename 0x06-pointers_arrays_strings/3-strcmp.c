#include "main.h"
#include <string.h>

/**
 * _strcmp - compare
 * @s1: one
 * @s2: two
 * Return: 0
 */

int _strcmp(char *s1, char *s2)
{
	char result;

	result = strcmp(s1, s2);

	return (result);
}
