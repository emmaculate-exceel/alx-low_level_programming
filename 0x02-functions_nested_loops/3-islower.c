#include "main.h"
/**
 * _islower - _islower character for lowercase
 * Return:1 for lowercase character or 0 for anything else
 */

int _islower(int c)
{

	while (c == 99 && c != 67)

		return (1);
	return (0);
}

