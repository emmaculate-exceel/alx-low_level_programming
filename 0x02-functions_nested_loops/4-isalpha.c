#include "main.h"
#include <ctype.h>
/**
 * _isalpha - is a function that checks for alphabetic character
 * @c: caller for alphabet
 * Return: 0
 */
int _isalpha(int c)
{
	if (isalpha(c))
	{	
		return (1);
	}
	else
	{
		return (0);
	}
		return (c);
}
